# schemas/

JSON schemas: agent-lock, workflow state, acceptances, review-metadata.

**Placeholder.** Schemas arrive in roadmap Phase B (state/lock extraction; review-metadata is
written new in Phase B).
