{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://provegate.dev/schemas/review-metadata.schema.json",
  "title": "Independent Review Artifact Metadata",
  "description": "Contract for the metadata block of an independent-review artifact (markdown blockquote header, parsed by core/gates review). Documented as JSON for tooling; the artifact itself is markdown. The `prd` pattern shows the DEFAULT id pattern; runtime validation derives it from workflow.config idPattern. Invariant: verdict `pass` requires `critical` = 0.",
  "type": "object",
  "required": ["prd", "verdict", "reviewer", "baseSha", "critical", "quorum"],
  "additionalProperties": false,
  "properties": {
    "prd": {
      "type": "string",
      "pattern": "^PRD-[0-9]{3}$"
    },
    "verdict": {
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "reviewer": {
      "type": "string",
      "minLength": 2,
      "description": "Tool/model identity of the reviewer \u2014 must not be the implementing agent."
    },
    "baseSha": {
      "type": "string",
      "minLength": 7,
      "description": "Merge-base (or base tip) the diff was reviewed against."
    },
    "critical": {
      "type": "integer",
      "minimum": 0
    },
    "high": {
      "type": "integer",
      "minimum": 0
    },
    "medium": {
      "type": "integer",
      "minimum": 0
    },
    "quorum": {
      "type": "string",
      "minLength": 3,
      "description": "Reviewer panel outcome, strictly \"N/M pass\" (N <= M, M >= 1). ENFORCED: a pass verdict requires N/M >= 3/5 in integer arithmetic (1/1 passes as the degenerate full-quorum case). See prompts/orchestration-runner.md for the panel protocol."
    }
  }
}
