/**
 * provegate — programmatic API.
 *
 * Bootstrap skeleton: the core modules are empty stubs until roadmap Phases B-C
 * extract state/locks (B) and the gate manifest + runner (C).
 */
export * from './core/index.js';
