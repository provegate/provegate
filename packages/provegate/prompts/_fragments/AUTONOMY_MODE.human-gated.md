This STOP has no exception. This repository is configured **human-gated**
(`prompts.values.AUTONOMY_MODE`): wait for the human's "Go", every time. The mode is
the configuration's statement — an agent never assesses which mode its own session is
in.
