# Summary: PRD-032 — Prompt-Store Dogfood

> **PRD**: [prd-032-prompt-store-dogfood.md](../../_prds/wip/prd-032-prompt-store-dogfood.md)
> **Status**: Code Complete — review passed; operator observation + acceptance pending
> **Ship Readiness**: Review Complete
> **Date**: 2026-07-29

## What shipped

This repository now runs its own prompt store. The `prompts` block carries all ten
owner-recorded values; the store (22 files), seven Claude commands, the Cursor rule
and the codex snippet are generated by the built CLI and committed — zero
hand-written bytes; `templates.prd` binds `gate new` to the rendered template;
PRD-034's reconciliation went live on the config flip alone and its bite is proven
by an executed mutation probe (planted byte named `modified`, restored, tree clean).
Two adopter-facing closures the dogfood surfaced: every generated Claude command now
lists phase-descriptively (the PRD-029 operator finding — banner intact and
parseable, `bannerVersion` exported as the one authority), and the review-template
Quorum contradiction is closed at its SOURCE (both copies required-Quorum, a
standing semantic verifier as a CHECKS member, pack-drift reconciled).

## The review

Two independent Codex rounds (Critical 0 both; one High closed: the probe's
false-green assertion). Three negative mutations executed: a nulled value refuses
the render, the Quorum regression fails by name, an unwired verifier is caught.

## Pending for close (operator-gated)

Task 8.11 — the live observation: `/prd-*` commands list phase-descriptively in
Claude Code's palette. Phase-7 owner acceptance names it.
