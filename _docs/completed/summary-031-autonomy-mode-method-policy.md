# Development Summary: Autonomy Mode and the Phase 4–7 Proceed Rule

> **PRD**: [prd-031-autonomy-mode-method-policy.md](../../_prds/completed/prd-031-autonomy-mode-method-policy.md)
> **Tasks**: [tasks-031-autonomy-mode-method-policy.md](../../_tasks/completed/tasks-031-autonomy-mode-method-policy.md)
> **Ship Readiness**: Operator Verification
> **Completed**: 2026-07-28
> **Author**: Claude Fable 5 (implementation), Codex ×3 rounds (independent review), owner (addendum approval)

---

## Overview

Two method rules that worked against the autonomy boundary are fixed at their owners.
The Phase 3 STOP rule's self-granted exception becomes `{{AUTONOMY_MODE}}` — the
package's first enumerated token: the human records `human-gated` or `autonomous` in
configuration, two shipped fragments carry the only legal renderings (the autonomous
one restores the snapshot's dropped parenthetical), and no rendering ever asks the
agent which mode its session is in. The entrypoint and its shipped template gain the
proceed rule beside the ten stop rules — during Phases 4–7 the only legitimate stops
are the enumerated checkpoints and a failed gate — identical by block-compare, with the
orchestration protocol stating the same rule for the phases it drives.

## Provenance

Every changed method byte traces to **Addendum A2**
(`source-snapshot/addenda/autonomy-mode-and-proceed-rule-2026-07-27.md`), drafted by
the agent, **approved by the owner in-session before any method byte moved**, sealed in
its status line and listed in the snapshot manifest. The frozen snapshot is untouched.
The package test proves artifact shape and the two authorized clauses only — approval
is the operator row plus the close acceptance naming the exact path.

## Verification

Registry 21 rows / one enumeration / TEN required values; both-mode renders, illegal-key
refusal, temp-copy missing-fragment mutation, terminality, sanctioned-sentence-stripped
self-assessment denylist with its own doctored-fragment mutation; two-copy identity by
block extraction. Suites 43+23+83+9 green; floor 7/7; `verify:workflow` PASS. Codex
review three rounds — the W2 boundary bounded honestly, the snapshot anchor extended to
the full sentence, and a Deferrals entry that two silent str.replace no-ops had dropped
(Codex caught it twice) landed with an assert.
