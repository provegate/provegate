# \_state/

Workflow state for this repo's own gated development.

- `schema/` — JSON schemas for state artifacts. Empty until roadmap Phase B extracts them
  (lifecycle, lock, acceptances, review-metadata).
- `locks/` — agent lock leases. Ephemeral, gitignored, never committed.
