# \_state/schema/

Source of truth for state-artifact schemas is the package:
[`packages/provegate/schemas/`](../../packages/provegate/schemas/) — agent-lock, prd-state,
acceptances. This repo dogfoods those schemas directly; nothing is duplicated here.
