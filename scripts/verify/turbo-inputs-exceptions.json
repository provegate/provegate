{
  "test": "PRD-024 FR-2: the provegate corpus test reads repository-root inputs the package-default hash set cannot see \u2014 _prds/** (the corpus), workflow.config.json and gates.manifest.json (the five-argument production lint call resolves both), and \u2014 PRD-028 FR-3 \u2014 _brain/** (the memory store `lintPrd` loads when memory is enabled; a record edit outside the cache key replayed a stale corpus green). $TURBO_DEFAULT$ keeps the package inputs. Delete this entry when the corpus test is removed."
}
