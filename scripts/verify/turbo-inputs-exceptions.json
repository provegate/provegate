{
  "test": "PRD-024 FR-2: the provegate corpus test reads repository-root PRDs under _prds/** — a surface the package-default hash set cannot see. $TURBO_DEFAULT$ keeps the package inputs; $TURBO_ROOT$/_prds/** adds the corpus. Delete this entry when the corpus test is removed."
}
