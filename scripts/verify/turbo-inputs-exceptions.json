{
  "test": "PRD-024 FR-2: the provegate corpus test reads repository-root inputs the package-default hash set cannot see \u2014 _prds/** (the corpus), workflow.config.json and gates.manifest.json (the five-argument production lint call resolves both), PRD-028 FR-3: _brain/** (the memory store `lintPrd` loads when memory is enabled), and \u2014 PRD-036 FR-1, the complete measured census \u2014 **/*.md (the consolidation liveMarkdown walk plus every direct .md read incl. AGENT_BOOTSTRAP/README/STATUS), docs/research/provegate-bootstrap/** (the frozen source snapshot digests and the whitepaper/roadmap reads), scripts/verify/** (the real-bundle wiring fixture, the cpSync'd doc-claims sources, the exec'd script-classes ledger), _docs/reviews/** (the review-quorum retro-validation), apps/docs/content/docs/** (the content-adoption/revalidate/content-launch page reads), .changeset/** (changeset-entry and prompts-integrity), package.json + turbo.json + LICENSE (root-manifest reads), apps/web/app/page.tsx (the content-launch self-copy page), and .github/workflows/** + .githooks/** (auditWiring's transitive reads under practices-pack). $TURBO_DEFAULT$ keeps the package inputs. The **/*.md superset is deliberate: a live-markdown edit re-runs every workspace's test task \u2014 a cheap re-run beats a stale green inside a gate close. verify:test-inputs enforces ledger\u2194turbo agreement in both directions. Delete entries only with their reading tests."
}
