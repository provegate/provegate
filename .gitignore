# dependencies
node_modules/

# build output
dist/
.next/
out/
.turbo/
.source/

# workflow runtime state (leases ephemeral, metrics local-only — never committed)
_state/locks/
_state/prd-metrics.jsonl

# gate --worktree linked checkouts (provisioned under worktree.dir, never committed)
.worktrees/

# logs & OS noise
*.tsbuildinfo
*.log
.DS_Store

# env — all real env files; examples stay committed
.env
.env.*
!.env.example
.gstack/
