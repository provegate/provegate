# PRD-XXX: [Feature Adi]

> **Status**: Draft | In Review | Approved | In Progress | Code Complete | Operator Verification | Ship Verified | Superseded | Blocked
>
> <!-- Yalnız bu kanonik değerler. "Completed"/"Done" YAZMA — verify:prd-state PRD-198'den itibaren alias'ları reddediyor. -->
>
> **Created**: [YYYY-MM-DD]
> **Updated**: [YYYY-MM-DD]
> **Author**: [Isim]
> **Audience**: Implementing Agent (Claude Code / Cursor / Codex)
> **Slug**: `[short-name]`
> **Cycle Phase**: 1 (PRD Generation)
> **PRD Class**: feature | test-hardening | hotfix | infra (see classes below; default `feature`)
> **Class Rationale**: [class ≠ feature ise zorunlu — neden bu sınıf, neden feature değil; tek cümle]
> **Autonomous Close**: eligible | operator-gated
> **State Record**: `_state/prds.json` (generated by `pnpm state:sync`)

<!--
Autonomous Close (PRD-248'den itibaren zorunlu; verify:prd-ready PRD≥248'de
mekanik kontrol eder) — 7-fazlı gated otonom akışta bu PRD'nin otonom
kapanmaya uygun olup olmadığını deklare eder:

- `eligible` — tüm doğrulama makine-checkable; operator-owned/blocked iş YOK.
  Orchestration tüm gate'ler yeşilse PRD'yi otonom kapatıp lokal development'a
  merge edebilir (push yine insanda).
- `operator-gated` — human/runtime/staging/secret doğrulaması gerekiyor.
  Orchestration `Operator Verification`'da durur; owner-imzalı Operator
  Acceptance + owner-gated `prd:accept` olmadan `Ship Verified` olamaz.

Operator-owned satır üreten her PRD `operator-gated` OLMAK ZORUNDA. `eligible`
işaretleyip sonra operator row eklemek close-zamanı verify:prd-state'te yakalanır.
-->

<!--
PRD Class — choose one. Phase 1/2/2b prompts and verify:prd-ready
branch on this field; pick honestly or the workflow over- or
under-engineers the work:

- `feature` — new user-facing capability, schema change, RBAC update,
  cross-module integration, or anything that ships a new product
  surface. Phase 2 scores all 6 dimensions; Phase 2b uses the
  Database → Backend → API → Events → Frontend → Permissions → QA
  parent skeleton.
- `test-hardening` — single-test or test-infra fix, no production
  code changes. Phase 2 marks Multi-Tenancy & Security (20%) and
  Migration & Rollback (10%) as N/A and redistributes their weight
  to Clarity (25%) + Completeness (30%) + Tech Depth (33%) + Scope
  (12%). Phase 2b uses Diagnostic → Fix → Audit → Doc → Gate
  skeleton. verify:prd-ready skips the project-wide
  OrgScopedRepository / "owner" / PERMISSION_MATRIX boilerplate
  requirement.
- `hotfix` — production bug fix with bounded blast radius. Phase 2
  marks only Migration & Rollback N/A; Multi-Tenancy stays scored at
  10%. From PRD-199 verify:prd-ready applies the full DO NOT
  boilerplate (a hotfix touches production code by definition).
  Phase 2b uses Repro → Fix → Verify → Doc → Gate skeleton.
- `infra` — workflow / tooling / CI / deploy change. Phase 2 keeps
  the 6-dim formula but inflates Migration & Rollback to 20%
  (deployment ordering is the real risk). Phase 2b emphasises
  Rollback Plan + Verification Ledger over Backend/Frontend parents.
-->

---

## 1. Introduction / Overview

[Feature'in kisa aciklamasi ve cozdugu problem]

---

## 2. Goals

### Primary Goals

- [ ] Goal 1
- [ ] Goal 2
- [ ] Goal 3

### Success Metrics

| Metric     | Current | Target | Measurement      |
| ---------- | ------- | ------ | ---------------- |
| [Metric 1] | [X]     | [Y]    | [How to measure] |
| [Metric 2] | [X]     | [Y]    | [How to measure] |

---

## 3. User Stories

### Primary User: [User Type]

#### User Story 1

```
As a [user type],
I want to [action],
so that [benefit].
```

**Acceptance Criteria:**

- [ ] AC1
- [ ] AC2
- [ ] AC3

#### User Story 2

```
As a [user type],
I want to [action],
so that [benefit].
```

**Acceptance Criteria:**

- [ ] AC1
- [ ] AC2

---

## 4. Functional Requirements

Each FR carries the exact target paths the implementing agent will touch. Use `path/to/file.ts::SymbolName` notation for symbol-level targets.

1. **FR-1**: [Requirement description]
   - **Targets:** `path/to/file.ts::SymbolName`, `path/to/other.ts`
2. **FR-2**: [Requirement description]
   - **Targets:** ...
3. **FR-3**: [Requirement description]
   - **Targets:** ...
4. **FR-4**: [Requirement description]
   - **Targets:** ...
5. **FR-5**: [Requirement description]
   - **Targets:** ...

---

## 5. Non-Goals (Out of Scope)

- [Bu PRD kapsaminda yapilmayacak 1]
- [Bu PRD kapsaminda yapilmayacak 2]

---

## 6. Design Considerations

### UI/UX Requirements

- [Requirement 1]
- [Requirement 2]

### Mockups / References

- [Link to mockup or description]

---

## 7. Technical Considerations

### Architecture

[Mimari yaklasim]

### Dependencies

- [Dependency 1]
- [Dependency 2]

### Database Changes

```sql
-- Gerekli schema degisiklikleri
```

### API Changes

| Method | Endpoint | Description |
| ------ | -------- | ----------- |
| POST   | /api/... | ...         |
| GET    | /api/... | ...         |

---

## 8. Implementation Scope

### In Scope

- [ ] Web (`apps/web/`)
- [ ] Native (`apps/native/`)
- [ ] API (`packages/api/`)
- [ ] Database (`packages/db/`)
- [ ] Auth (`packages/auth/`)

### Phases (Optional)

**Phase 1:**

- [Deliverable 1]
- [Deliverable 2]

**Phase 2:**

- [Deliverable 1]
- [Deliverable 2]

---

## 9. Open Questions

- [ ] Question 1?
- [ ] Question 2?

---

## 10. References

- [Link 1]
- [Link 2]

---

## Conflict Surface

Bu PRD'nin **dışlayıcı yazma sahipliği** iddia ettiği kaynak yolları (glob). `prd:start`
bunları lock'un `ownedPaths` alanına aynalar; `verify:path-conflicts` iki AKTİF execution-faz
lock çakışan path iddia ederse FAIL eder (PRD-312). İddia yoksa `- none` yaz.

> **Kural:** Paylaşılan/append-only manifest'leri (`package.json`, `pnpm-lock.yaml`,
> `docs/ai-context/WORKFLOW.md`, `scripts/verify-workflow.mjs`, `_STATUS.md`, `CLAUDE.md`,
> `AGENTS.md`) **ASLA** burada deklare etme — onlar append-only sınıf (git union merge-driver
> çözer). Yalnız bu PRD'nin sahiplendiği **kaynak** dosya/dizinleri listele.

- `path/to/owned/dir/**`
- `path/to/file.ts`
- `none`

---

## Durable Artifacts

Bu PRD'nin durable bilgisi nereye iniyor (Faz 7 — Learning — gate bunları kontrol
eder: `none` olmayan her path merge diff'inde dokunulmuş olmalı). Boş bırakma —
hiçbir şey yoksa `none` yaz. Kapsam dar: yalnız **bu PRD'nin** durable bilgisi
(cross-wave retro değil).

- Wiki: `docs/ai-context/wiki/{page}.md` — [ne öğrenildi] | `none`
- ADR: `docs/ai-context/decisions/adr-XXX-*.md` (yeni karar) | `none`
- Pattern: `docs/ai-context/patterns/{pattern}.md` | `none`

---

## 11. Verification Commands

Commands the implementing agent runs to confirm completion. **Her FR-N için tabloda
en az bir satır ZORUNLU**, ve o satır backtick'li çalıştırılabilir bir komut
taşımalı (`pnpm …` / `node …` / `tsx …` / `vitest …` / `playwright …` / `psql …` /
`curl …` / `test -f …` / `grep …`). Bu, Faz 5 (Testing) gate'ini makine-checkable yapar:
orchestration §4'teki her FR'ı §11'de eşleşen komutla doğrular ve her komut
exit 0 vermeden faz geçmez. (PRD-248'den itibaren `verify:prd-ready` PRD≥248'de
FR↔komut eşleşmesini mekanik kontrol eder; `test-hardening` sınıfı gevşektir.)

| FR   | Command / Check | Scope | Notes |
| ---- | --------------- | ----- | ----- |
| FR-1 | `pnpm ...`      |       |       |

- `pnpm check-types` — zero errors
- `pnpm lint --filter <scope>` — zero warnings on touched packages
- `pnpm test --filter <scope>` — added tests pass; existing tests unchanged
- `pnpm build --filter <scope>` — clean build
- [Feature-specific: e.g., `pnpm --filter @emofy/db db:migrate`, `pnpm verify:ema-manifest`, curl probe]

Hard caps (calibration 2026-06-10; PRD-199'dan itibaren `verify:prd-ready` mekanik kontrol eder):

- Cross-tenant test: `path/to/x.cross-tenant.e2e-spec.ts` — [test adı; Targets'ta backend controller varsa ZORUNLU]
- Contract test: `path/to/x.contract.spec.ts` — [dto/schema + frontend target birlikteyse ZORUNLU]

**Class-default gates (PRD-249):** `prd:autorun` Phase 4 auto-runs extra checks by PRD Class + diff (`verify:affected-tests`, `verify:gates-wired` for infra, `verify:rds-imports` for UI apps, etc.). List them in §11 only when you need explicit ledger evidence — do not duplicate unless required for operator audit.

Before Phase 2 PASS, run:

- `pnpm verify:prd-ready -- _prds/wip/prd-XXX-{short-name}.md`

---

## 12. DO NOT (Anti-Patterns)

Explicit forbidden moves for this PRD. Catches drift the agent might otherwise rationalize.

- DO NOT introduce `any`; use `unknown` + narrowing.
- DO NOT bypass `OrgScopedRepository` (no raw `db.select()` on tenant tables).
- DO NOT invent role strings outside the canonical 9-role union; ghosts `"workspaceManager"` / `"workspaceAdmin"` are invalid; `owner` is canonical, never a ghost.
- DO NOT add new permission strings outside `PERMISSION_MATRIX` in `@emofy/types`.
- [Feature-specific anti-patterns]

---

## Changelog

| Date         | Author | Changes       |
| ------------ | ------ | ------------- |
| [YYYY-MM-DD] | [Name] | Initial draft |
